@model IEnumerable<Meditrack.Models.OcupacionMedica>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreOcupacion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DescripcionOcupacion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreOcupacion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DescripcionOcupacion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdOcupacion">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdOcupacion">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdOcupacion">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
