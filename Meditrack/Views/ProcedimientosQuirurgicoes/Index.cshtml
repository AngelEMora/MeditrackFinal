@model IEnumerable<Meditrack.Models.ProcedimientosQuirurgico>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreProcedimiento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Descripcion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RiesgosComplicaciones)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DuracionEstimada)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreProcedimiento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RiesgosComplicaciones)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DuracionEstimada)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdProcedimiento">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdProcedimiento">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdProcedimiento">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
