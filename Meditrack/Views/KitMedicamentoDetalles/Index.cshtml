@model IEnumerable<Meditrack.Models.KitMedicamentoDetalle>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NotasMedicMedicamento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CantidadMedicamento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdKitsMedicamentoNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdMedicamentoNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NotasMedicMedicamento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CantidadMedicamento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdKitsMedicamentoNavigation.IdKitsMedicamentos)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdMedicamentoNavigation.IdMedicamento)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.KitMedicamentoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.KitMedicamentoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.KitMedicamentoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
