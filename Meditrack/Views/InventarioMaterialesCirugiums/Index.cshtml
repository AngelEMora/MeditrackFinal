@model IEnumerable<Meditrack.Models.InventarioMaterialesCirugium>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CantidadUtilizadaMaterialCirugia)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdCirugiaNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdKitsCirugiasNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CantidadUtilizadaMaterialCirugia)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdCirugiaNavigation.IdCirugia)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdKitsCirugiasNavigation.IdKitsCirugias)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdInventario">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdInventario">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdInventario">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
