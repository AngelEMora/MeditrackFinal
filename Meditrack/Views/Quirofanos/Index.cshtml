@model IEnumerable<Meditrack.Models.Quirofano>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreQuirofano)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ubicacion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DescripcionEstado)
            </th>
            
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreQuirofano)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ubicacion)
            </td>
            
            <td>
                @Html.DisplayFor(modelItem => item.DescripcionEstadoQuirofano)
            </td>
            
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdQuirofano">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdQuirofano">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdQuirofano">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
