@model IEnumerable<Meditrack.Models.TipodeMaterialQuirurgico>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreTipoMaterialQuirurgico)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DescripcionTipoMaterialQuirurgico)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EstadoMaterialNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreTipoMaterialQuirurgico)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DescripcionTipoMaterialQuirurgico)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EstadoMaterialNavigation.IdEstadoMaterialQuirurico)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTipoMaterialQuirurgico">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdTipoMaterialQuirurgico">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdTipoMaterialQuirurgico">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
